apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "workshop-setup-mcp.fullname" . }}-config
  labels:
    {{- include "workshop-setup-mcp.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
