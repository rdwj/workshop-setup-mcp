{{- if .Values.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "workshop-setup-mcp-gateway.fullname" . }}
  labels:
    {{- include "workshop-setup-mcp-gateway.labels" . | nindent 4 }}
spec:
  to:
    kind: Service
    name: {{ include "workshop-setup-mcp-gateway.fullname" . }}
    weight: 100
  port:
    targetPort: http
  tls:
    termination: {{ .Values.route.tls.termination }}
    insecureEdgeTerminationPolicy: {{ .Values.route.tls.insecureEdgeTerminationPolicy }}
{{- end }}
