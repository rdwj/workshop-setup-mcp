apiVersion: v1
kind: Service
metadata:
  name: {{ include "workshop-setup-mcp-gateway.fullname" . }}
  labels:
    {{- include "workshop-setup-mcp-gateway.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workshop-setup-mcp-gateway.selectorLabels" . | nindent 4 }}
